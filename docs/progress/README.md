# Progress captures

One entry per capture, newest last. Each file is named `<date>-<ticket>-<screen>-<state>.png`.

- `2026-09-02-issue-13-attendees-requests.png`: the Attendees tab after #13 with three attendees, one complete. Shows two defects: the seeded "Name for printing" column reads missing on every row (#18), and attendees who replied after the request went out have no request row.
- `2026-09-02-issue-13-attendees-requests-after-fix.png`: the same screen after the late-attendee fix. The request column and the follow-up count now cover every going attendee.
- `2026-09-02-issue-18-attendees-no-seeded-name.png`: the Attendees tab after #18. The grid holds only the store's requirements and the request state; the seeded name column is gone.
- `2026-09-02-issue-07-home.png`: the root route after #7, full page: hero, the problem, the five-step walkthrough with the recaptured clips, the three tool cards, and the closing band.
- `2026-09-02-issue-20-tour-attendees.png`: the demo tour after #20 on the Attendees tab mid-run, full page: the spotlight on the request action and the callout with the step counter, the organizer's narration, Back, Next, and the autoplay toggle.
- `2026-09-02-issue-20-tour-production-checkout.png`: the guest tour on https://tokuchu.onrender.com at its final step, the checkout link ready after 108 s of live actions.
- `2026-09-02-issue-22-tour-wire-search.png`: the tour's search step after #22 while the search runs: the callout pinned to the results region with the wire block under the narration listing the Global Catalog endpoint, the demo store's UCP endpoint, the `search_catalog` tool, and the sentence in flight.
- `2026-09-02-issue-22-tour-wire-cart.png`: the cart step after #22 while the store fills the cart: the callout pinned to the approve block with `approve_specs`, `add_customized_to_cart` with the item count, and the cart job's first post read from the gift's updates thread. The routing panel behind it names the demo store built for this project.
- `2026-09-02-home-banner.png`: the homepage after the banner and palette change, full page: cream field, navy ink, orange on the two primary actions only.
- `2026-09-02-home-usecase.png`: the Customworks use-case section with the store's banner and the paragraph on its callable customization tool.
- `2026-09-02-home-contract.png`: the merchant's contract section: what Customworks returns, the source Tokuchu resolves per field, and the attendee form generated from it.
- `2026-09-02-home-problem.png`: the problem section with the scale cards, three attendees in the demo against 1,500 values at a real event.
- `2026-09-02-home-parties.png`: the three-parties section: Shopify WebMCP with its ten page tools, Customworks with the store screenshot and its two tools, Tokuchu with the Attendees tab and its page tools.
- `2026-09-02-tour-intro-desktop.png` and `2026-09-02-tour-intro-mobile.png`: the tour's opening story, Jack beside a telescope in ASCII with the first beat typed out.
- `2026-09-02-issue-27-contract-before.png`: the contract section before issue 27 with the first two columns stretched to the form mock and a blank block under their last rows.
- `2026-09-02-issue-27-contract-after.png`: the contract section after issue 27 with each column ending at its last row and the three tops still aligned.
- `2026-09-02-issue-28-header-before.png`: the signed-in home header at 1440 and 1280 wide with the anchors breaking inside their labels and Sign out and Create an event on two lines.
- `2026-09-02-issue-28-header-after.png`: the same header after the fix: every label on one line and the address pill truncated with an ellipsis.
- `2026-09-03-issue-31-chat-desktop.png`: the event page at 1440 wide after #31 with the assistant panel as a column beside the Overview: one organizer line and the reply from a real turn with the two tools it called listed under it. The reply still carries markdown markers; the instruction to write plain text went in after this capture.
- `2026-09-03-issue-31-chat-mobile.png`: the same thread at 390 wide as the drawer the Assistant button opens over the page.
- `2026-09-03-issue-35-attendees-three-gifts.png`: the Attendees tab after #35 with three gifts at the approve step: the gift switcher naming the crewneck and the mug from the demo store and the food gift from its store, the crewneck's requirements, and the records grid with that gift's columns filled for the three attendees.
- `2026-09-03-issue-35-tour-wire-mug.png`: the tour's mug step after #35 with the wire block listing the store's `get_customization` call for the mug and the one image field it answered.
- `2026-09-03-issue-35-tour-wire-food.png`: the tour's food step after #35 while the pick runs: the wire block with `search_catalog` in the food and beverage category, `get_product` for the chosen box, no customization tool, and the store's UCP `create_cart` and `create_checkout` endpoint. The order summary behind it lists the three gifts from two stores.
- `2026-09-03-issue-47-store-page-desktop.png`: the store-facing page at 1440 wide after #47, opened through the signed link for a grant with every permission: the Procurement's status and revisions, the requirement schema and the access the grant carries, the three requirements, the manifest with one incomplete row, the empty exceptions list, the accepted update the form just posted, and the form.
- `2026-09-03-issue-47-store-page-mobile.png`: the same page at 390 wide with the stat cards in two columns and the manifest scrolling inside its own frame.
- `2026-09-03-issue-44-stale-approval.png`: the Attendees tab at 1440 wide after #44 with a stale approval: the Approved pill beside a Re-approve action, the line naming the approved revision and the revision that has changes since, the count of attendees who changed an answer, and the cart job's report that no row was ready. The organizer approved, then the attendee changed the star map location through the RSVP endpoint.
- `2026-09-03-issue-46-share-block.png`: the Attendees tab at 1440 wide after #46 with the Share fulfilment data block under Fill the cart: the store name read from the gift, the four access checkboxes, the expiry set to after fulfilment, and the grant the organizer just created as Active with the two fields and the one fulfilment record it reaches beside Copy access link and Revoke.
- `2026-09-03-issue-49-readiness-grid.png`: the Attendees block at 1440 wide after #49 with the Procurement's status and revisions above the records grid and every cell graded from the fulfilment manifest: a resolved location, a caption over the store's 20-character limit as invalid with the constraint named, a missing location with the store's question under it, a caption the store rejected with its message, an engraving column left mapping unresolved by a re-read schema, and a location changed after the approval. The legend under the grid explains each state and the exceptions list carries the two open exceptions with their correction requests sent.
