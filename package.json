{
  "name": "tokuchu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/shopify-ucp"
  ],
  "scripts": {
    "dev": "next dev -p 3113",
    "build": "next build",
    "start": "next start -p 3113",
    "test": "vitest run && npm run test --workspaces --if-present",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && npm run typecheck --workspaces --if-present",
    "test:e2e": "playwright test",
    "test:customily-demo": "playwright test tests/customily-demo.spec.ts",
    "setup": "bash scripts/setup.sh"
  },
  "dependencies": {
    "@openai/agents": "^0.17.0",
    "@webmcp/shopify-ucp": "*",
    "next": "^16.3.3",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.11",
    "webmcp-types": "^0.1.5"
  }
}
